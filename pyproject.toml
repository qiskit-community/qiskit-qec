# copied pasted from Qiskit Terra 2021
[build-system]
requires = ["setuptools", "wheel", "pybind11~=2.6.1"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py36', 'py37', 'py38', 'py39']
